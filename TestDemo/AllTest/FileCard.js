import React, { useState } from 'react';
import { 
    View, 
    Text,
    FlatList, 
    ActivityIndicator,
    StyleSheet,
    Image,
    TouchableOpacity,
    TextInput
} from 'react-native';

export default function FileCard() {
  
    function item(){
        const [show, hide] = useState(-1);
    }
    

return(
    
    
);
}
